# Legacy HISCALE example

This directory preserves the previous HISCALE example as temporary source material for the preprocessing workflow.

It is not the public entry point for the HISCALE April 25, 2016 case. Use `../run_case.py` and the documentation in `../README.md` for the supported case workflow.
